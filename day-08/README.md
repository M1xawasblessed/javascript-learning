## Day 8: Array Methods

**Today's Progress:**
- Learned how to manipulate arrays using built-in JavaScript methods.
- Used `.map()` to create a new array by transforming each element.
- Used `.filter()` to extract specific elements that meet a given condition.
- Used `.forEach()` to execute a function for every element in the array.
- Practiced basic array operations like `.push()` (add to end) and `.pop()` (remove from end).