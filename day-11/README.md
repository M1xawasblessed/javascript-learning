## Day 11: Async/Await & Fetch API

**Today's Progress:**
- Learned the modern way to handle asynchronous operations using `async` and `await`.
- Used the `fetch()` API to make HTTP requests and get data from an external server.
- Handled JSON responses using the `.json()` method.
- Implemented `try...catch...finally` blocks to effectively manage errors and run cleanup code.