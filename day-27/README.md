## Day 27: Mini Project - Weather App

**Today's Progress:**
- Built a weather application that fetches real-time data from a public API.
- Practiced using the `Fetch API` alongside `async` and `await` for cleaner asynchronous code.
- Chained multiple API requests (Geocoding API to get coordinates, then Weather API for the forecast).
- Implemented error handling (`try...catch`) to catch invalid city names or network issues.
- Dynamically rendered the fetched API data into the DOM.