## Day 28: Mini Project - Random User Generator

**Today's Progress:**
- Built a web application that generates random user profiles by fetching data from the Random User API.
- Reinforced knowledge of the `Fetch API` and asynchronous JavaScript (`async`/`await`).
- Handled deeply nested JSON structures to extract specific details like name, email, location, and profile picture.
- Dynamically updated the DOM by injecting HTML structured data and rendering external images.