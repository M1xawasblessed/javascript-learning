## Day 20: ES6 Modules

**Today's Progress:**
- Learned how to organize code by splitting it into multiple files using ES6 Modules.
- Practiced exporting variables and functions using Named Exports (`export const ...`).
- Used Default Exports (`export default ...`) for the primary function of a module.
- Imported modules into another file using the `import` statement to reuse code efficiently.