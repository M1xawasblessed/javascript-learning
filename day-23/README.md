## Day 23: Regular Expressions (Regex)

**Today's Progress:**
- Learned the syntax of Regular Expressions.
- Used regex flags like `i` for case-insensitive and `g` for global searches.
- Applied character classes and quantifiers to match specific string patterns.
- Tested strings using `.test()` and extracted data using `.match()`.
- Implemented a basic email validation pattern.