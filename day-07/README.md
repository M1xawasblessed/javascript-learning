## Day 7: Objects and Methods

**Today's Progress:**
- Learned how to group related data and functions together using Objects (key-value pairs).
- Understood how to access, add, update, and delete properties using dot notation (`object.property`) and bracket notation (`object["property"]`).
- Created object methods and explored the use of the `this` keyword to reference the object's own properties internally.