## Day 17: Event Listeners

**Today's Progress:**
- Learned how to make web pages interactive using `addEventListener()`.
- Handled mouse events like `click` to trigger specific functions and UI changes.
- Tracked user input in real-time using the `input` event and the `event` object (`event.target.value`).
- Prevented default browser behaviors, such as page reloads on form submissions, using `event.preventDefault()`.