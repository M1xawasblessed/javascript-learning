## Day 25: BOM (Browser Object Model)

**Today's Progress:**
- Interacted with the `window` object to get browser dimensions and trigger alerts/confirmations.
- Used the `navigator` object to detect browser language, online status, and user agent.
- Explored the Geolocation API to fetch user coordinates.
- Manipulated the browser's URL and navigation using the `location` object.