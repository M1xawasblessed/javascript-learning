## Day 18: Advanced Events (Bubbling & Delegation)

**Today's Progress:**
- Explored the event propagation flow in the DOM, focusing on Event Bubbling.
- Used `event.stopPropagation()` to stop an event from traveling up the DOM tree and triggering parent listeners.
- Implemented Event Delegation to handle events efficiently by attaching a single event listener to a parent element.
- Utilized `event.target` and `.tagName` to identify exactly which child element triggered the event within the parent container.