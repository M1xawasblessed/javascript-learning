## Day 3: Control Flow (if / else)

**Today's Progress:**
- Learned how to make decisions in code using `if`, `else if`, and `else` statements.
- Practiced with comparison operators (like `>=`, `>`, etc.).
