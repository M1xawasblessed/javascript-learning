## Day 12: Classes and Object-Oriented Programming (OOP)

**Today's Progress:**
- Introduced to Object-Oriented Programming in JavaScript using the `class` syntax.
- Created a base class with a `constructor` function to initialize object properties.
- Added instance methods to the class.
- Learned about class inheritance by creating a subclass using the `extends` keyword.
- Used `super()` to call the parent class's constructor.