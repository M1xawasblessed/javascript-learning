## Day 16: Manipulating the DOM

**Today's Progress:**
- Learned how to dynamically create HTML elements using `document.createElement()`.
- Appended newly created elements to the webpage using `appendChild()`.
- Managed CSS classes dynamically with the `classList` property (`add`, `remove`, `toggle`).
- Explored how to completely remove elements from the DOM using `.remove()`.