## Day 1 & 2: JavaScript Basics

**Today's Progress:**
- Learned how to display output in the terminal using `console.log()`.
- Declared variables using the `let` keyword.
- Created and invoked a simple function with arguments.

**Notes:**
Moved the actual JavaScript code to `index.js` to run it via Node.js. Getting familiar with the syntax!