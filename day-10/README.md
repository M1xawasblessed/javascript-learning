## Day 10: Asynchronous JavaScript (setTimeout & Promises)

**Today's Progress:**
- Explored the asynchronous nature of JavaScript.
- Used `setTimeout()` to execute code after a specified delay without blocking the rest of the program.
- Learned how to create a `Promise` to handle asynchronous operations.
- Used `.then()` to handle a successfully resolved promise and `.catch()` to handle any rejections or errors.