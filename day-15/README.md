## Day 15: DOM Selection and Basics

**Today's Progress:**
- Learned what the Document Object Model (DOM) is and how it connects HTML with JavaScript.
- Practiced selecting HTML elements using `getElementById` and `getElementsByClassName`.
- Mastered modern selection methods using `querySelector` and `querySelectorAll`.
- Explored how to modify an element's text content and basic CSS styling directly from JavaScript.